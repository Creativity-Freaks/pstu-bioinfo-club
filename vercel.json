{
  "buildCommand": "bun run build || npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/admin", "dest": "/admin.html" },
    { "src": "/.*", "dest": "/index.html" }
  ]
}
